#!/bin/bash

echo -n "Enter a word >"
read userword
case $userword in
                # check for numbers
        [0-9]* ) echo "Entered number"
                ;;
                
                # check for any thing else
        * ) echo "Good entry"
        
esac 


 #!/bin/bash
    
    echo -n "Enter a word >"
    read userword
    case $userword in
                    # check for letters upper case and lower case
            [a-z] | [A-Z] ) echo "Entered letter" 
                    ;;
                    
                    # check for numbers
            [0-9] ) echo "Entered number"
                    ;;
                    
                    # check for any thing else
            * ) echo "Good entry"
            
    esac 




For particular _character_ check, say "A":



    #!/bin/bash
    
    echo -n "Enter a word >"
    read userword
    case $userword in
                    # check for letters upper case and lower case
            A ) echo "Entered letter" 
                    ;;
                    
                    # check for numbers
            [0-9] ) echo "Entered number"
                    ;;
                    
                    # check for any thing else
            * ) echo "Good entry"
            
    esac 


Check for "A" only in user input:


    #!/bin/bash
    
    echo -n "Enter a word >"
    read userword
    case $userword in
                    # check for letters upper case and lower case
            A ) echo "Entered letter" 
                    ;;
                                       
                    # check for any thing else
            * ) echo "Good entry"
            
    esac 
        
        
